# Growth Score — website prototype

DRAFT. Working entity name: Trigpoint (unconfirmed). Internal challenge only; not for external issue.

- Single static file: `index.html`. No build step, no dependencies.
- Rename the brand in ONE place: the `BRAND` variable at the top of the script block in `index.html`.
- Unverified figures render as marked placeholders by design. Do not replace with invented numbers.
- Rules baked in: no prices anywhere, no funding-probability claims, no credit/decisioning implication, UK English, no em/en dashes.

## Lovable
Connect this repo in the Lovable project's GitHub settings (or import it). Push edits here; Lovable syncs them without spending credits. Credits are only consumed by AI prompt edits inside Lovable.

## Hosting
Served free via GitHub Pages from main branch root.
